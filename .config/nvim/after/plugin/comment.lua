pcall(function()
    require('Comment').setup({
        toggler = {
            block = 'gCC'
        },
        opleader = {
            block = 'gC'
        },
        mappings = {
            basic = true,
            extra = false,
        }
    })
end)
